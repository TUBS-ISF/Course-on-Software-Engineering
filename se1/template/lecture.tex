\usepackage{../submodules/FancyBeamer/fancybeamer} % use the fancy beamer package
%\fancylogos{,uulm,,unibe,,ovgu-blue,}
\setfancycolumnsdefault{animation=keep} % animate all columns per default

% GRAPHICSPATH

\setpaths{
	{../pics/}
	{../submodules/Title-Pictures/}
	{../submodules/Course-on-Software-Product-Lines/pics/}
	% Florian Sihler's additions
	{../submodules/abstract-interpretation-ltx}
	{../submodules/code-animation}
	{../submodules/xlistings/langs}
}

% IMPORTED PACKAGES

\usepackage{multicol} % used temporarily for the lecture overview
\usepackage{stmaryrd} % \lightning in modeling lecture
\usepackage{catchfile} % read picture sources from files
\usepackage{silence} % ignore some warnings
\WarningFilter{latex}{You have requested package} % ignore warning for loading packages from submodules
%\usepackage{../EagleoutIce-fancyqr/fancyqr} % QR codes for practice task, credits to Florian Sihler
\usepackage{fancyqr} % QR codes for practice task, credits to Florian Sihler
\fancyqrset{gradient=true,gradient angle=-45,l color=black,r color=blue}
\usepackage{ifthen} % \ifuniversity
\usepackage{booktabs} % tables
%\usepackage{colortbl} % \rowcolor used in Lecture 8 in SPL
%\usepackage{algpseudocode} % used in Lecture 10 in SPL

% Florian Sihler's crazy additions because he ported his slides and tries to be a special snowflake
\usepackage{../submodules/abstract-interpretation-ltx/absint}
\usepackage{../submodules/code-animation/code-animation}
\usepackage{../submodules/xlistings/xlistings}
\makeatletter\LoadLanguages{java,R}
\usepackage{stmaryrd} % flo's symbols
\xlstsetmintedstyle{plain}
\def\Set#1{\ensuremath{\{#1\kern1pt\}}}
\def\absintstyle#1{#1}
\def\IntCC#1#2{\ensuremath{[#1\,..\,#2]}}
\def\IntOC#1#2{\ensuremath{(#1\,..\,#2]}}
\def\IntCO#1#2{\ensuremath{[#1\,..\,#2)}}
\def\IntOO#1#2{\ensuremath{(#1\,..\,#2)}}
\def\AbstractInfo#1{\ensuremath{\mathcolor{gray}{\Lbag}\,#1\,\mathcolor{gray}{\Rbag}}}
\usepackage{forest}

\usepackage{csquotes}
\usepackage[style=alphabetic,backend=biber]{biblatex}
\usepackage{multirow}
\usepackage{tikzpingus}
\usepackage{relsize}

% INCLUDED TEMPLATE FILES

\input{../se1/template/macros}
\input{../se1/template/listings} % TODO merge into template?
\input{../se1/template/slidetemplate} % TODO merge into template?
\input{../se1/content/literature}
\input{../se1/content/sharedslides}

\input{template/universities} % this file is specific to the respective lecture

% HOPEFULLY NON-PERMANENT HACKS

\usepackage{pgf, tikz}
\usetikzlibrary{arrows, arrows.meta, positioning, shapes, calc, decorations.text, shadows.blur, matrix, fit, mindmap, backgrounds}
\pgfdeclarelayer{foreground}
\pgfsetlayers{background,main,foreground}
\tikzset{ >=stealth'} % define standard arrow tip
\usepackage{tikz-qtree}

\newcommand{\handoutonly}[1]{}
\newcommand{\slideonly}[1]{#1}
\mode<handout>{
	\renewcommand{\handoutonly}[1]{#1}
	\renewcommand{\slideonly}[1]{}
}
\newcommand{\prehandout}[1]{#1}
\newcommand{\posthandout}[1]{}
\newcommand{\onlyhandout}[1]{\ifdefined\ishandout#1\fi}

\renewcommand{\prehandout}[1]{}
\renewcommand{\posthandout}[1]{#1}

% Keys to support piece-wise uncovering of elements in TikZ pictures:
% \node[visible on=<2->](foo){Foo}
% \node[visible on=<{2,4}>](bar){Bar}   % put braces around comma expressions
%
% Internally works by setting opacity=0 when invisible, which has the 
% adavantage (compared to \node<2->(foo){Foo} that the node is always there, hence
% always consumes space plus that coordinate (foo) is always available.
%
% The actual command that implements the invisibility can be overriden
% by altering the style invisible. For instance \tikzsset{invisible/.style={opacity=0.2}}
% would dim the "invisible" parts. Alternatively, the color might be set to white, if the
% output driver does not support transparencies (e.g., PS) 
%
\tikzset{
	invisible/.style={opacity=0},
	visible on/.style={alt={#1{}{invisible}}},
	alt/.code args={<#1>#2#3}{%
		\alt<#1>{\pgfkeysalso{#2}}{\pgfkeysalso{#3}} % \pgfkeysalso doesn't change the path
	},
}

\usepackage{tabularx}



% QR Code handling
\usepackage{fancyqr}
\makeatletter
\let\oldfancyqr\fancyqr
\renewcommand{\fancyqr}[2]{\renewcommand{\@minipagerestore}{\setlength{\parskip}{0pt}}\oldfancyqr[#1]{#2}} % TODO: enable []
\makeatother

\newcommand{\studipqr}{
	\fancyqr{color=black,height=3.5cm}{\StudIPLink}
}

